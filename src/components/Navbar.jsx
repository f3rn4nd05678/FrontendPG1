const NavBar = () => {
  return (
    <header className="bg-white shadow px-6 py-4 flex items-center justify-between">
      <h1 className="text-lg font-semibold text-gray-700">Panel Principal</h1>
      {/* Puedes agregar botón de logout, perfil, etc. */}
    </header>
  );
};

export default NavBar;
